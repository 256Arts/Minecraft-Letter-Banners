# Replaces the data pack's copy, which opens the dialog the data pack ships.
# The mod's dialog is built per player instead of registered, so the command is
# the only way in. See LetterBannerDialogs.java.
letterbanner
